# Сравнение AI-моделей для проверки лабораторных работ

Cравнение Transformer и CNN

# Преимущества трансформеров:

Параллельная обработка последовательности: Возможность обрабатывать последовательность параллельно благодаря механизму внимания (Attention). Это ускоряет обучение больших моделей.
Эффективность представления контекста: Механизм внимания позволяет эффективно учитывать дальние зависимости между элементами последовательности, что особенно полезно для задач NLP (Natural Language Processing).
Гибкость масштабирования: Благодаря параллельной обработке трансформеры хорошо подходят для тренировки на GPU и TPU, позволяя создавать большие модели.
Высокая производительность в задачах понимания естественного языка: Современные языки программирования, такие как OpenAI's GPT, Google's BERT и другие, основаны именно на архитектуре Transformer.

# Недостатки трансформеров:

Высокое потребление памяти: Поскольку механизм внимания требует хранения матричных произведений большого размера, использование крупных трансформеров становится ресурсоемким.
Проблемы оптимизации градиентов: Глубокий слой может привести к проблемам с оптимизацией (vanishing gradients problem), хотя это решается различными методами нормализации (LayerNorm, Residual Connections).
Чувствительность к порядку ввода: Хотя изначально внимание учитывало положение элементов относительно друг друга, порядок ввода важен для правильной интерпретации предложений.
📐 Основные характеристики сверточных нейронных сетей (CNN)

# Преимущества CNN:

Хорошее обобщение пространственных паттернов: Использование сверток позволяет выделить локальные особенности изображения или сигнала независимо от положения объекта внутри области восприятия.
Масштабируемость к большим размерам изображений: За счёт использования иерархии уровней свёрток и пуллинга возможно эффективное распознавание объектов на изображениях разного масштаба.
Автоматизация выделения признаков: CNN автоматически выделяет важные черты изображения, минимизируя необходимость ручного выбора признаков.
Низкое количество параметров по сравнению с полносвязанными сетями: Свёрточные слои значительно сокращают общее количество весов, снижая риск переобучения.

# Недостатки CNN:

Сложность работы с последовательностью: CNN плохо обрабатывают длинные последовательности, поскольку каждая операция свёртки имеет ограниченное окно восприятия.
Необходимость предварительной подготовки данных: Требуется предварительная нормализация и изменение формата данных, что усложняет процесс подготовки данных.
Зависимость от структуры сетки: Выбор подходящей архитектуры (глубины слоев, типа ядра свёртки и т.п.) является сложной задачей и зависит от конкретной задачи.

# Области применения:

Трансформеры активно используются в задачах обработки текста (языковые модели, классификация документов, машинный перевод), а также начинают применяться в компьютерном зрении и рекомендательных системах.
CNN чаще всего применяется в задаче классификации изображений (распознавание лиц, медицинских снимков, дорожных знаков), сегментации изображений и детекции объектов.
💡 Когда выбирать?
Если задача связана с последовательностями (текст, временные ряды), лучше выбрать трансформер. Например, для перевода текста или предсказания временных рядов.
Для задач компьютерного зрения, особенно для изображений, предпочтение отдаётся CNN, поскольку эта архитектура была специально разработана для эффективного извлечения признаков из двухмерных сигналов.
Таким образом, выбор нейросети должен зависеть от специфики решаемой задачи и особенностей ваших данных.
